const guest = {
  name: guestName,
  language: language,
};

function updateDisplay() {
  // Tangkap elemen yang dituju
    const displayName = document.getElementById("displayName");
    displayName.innerHTML = ('')
}

if (guest.language === "Indonesia" || "indonesia") {
}
